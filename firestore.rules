rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny by default; server SDK bypasses rules by using Admin privileges
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
